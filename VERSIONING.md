# 版本命名
## 版本号规范

`主版本号.次版本号.修订号`。

| 版本部分               | 何时递增                     | 示例                    |
|:-------------------|:-------------------------|:----------------------|
| **主版本号 (MAJOR)**   | 进行了不兼容的 API 修改或重大变更时。    | 从 `1.0.0` 变为 `2.0.0`  |
| **次版本号 (MINOR)**   | 增加了新功能，但保持向后兼容时。         | 从 `1.0.0` 变为 `1.1.0`  |
| **修订号 (PATCH)**    | 进行了向后兼容的问题修正 (bug 修复) 时。 | 从 `1.0.0` 变为 `1.0.1`  |

通过添加后缀来表示预发布版本，例如 `1.0.0-alpha.1`（内部测试）、`1.0.0-beta.1`（公开测试）、`1.0.0-rc.1`（发布候选版本）。

## 分支命名规则

分支名应该清晰地表达其用途，通常采用 `前缀/描述` 的格式。

*   **功能分支 (Feature)**：`feature/简短的功能描述`，例如 `feature/user-login-api`、`feature/add-shopping-cart`。
*   **错误修复分支 (Bugfix)**：`bugfix/描述的问题`，例如 `bugfix/fix-navbar-overflow`。
*   **热修复分支 (Hotfix)**：`hotfix/描述的问题`，用于紧急修复生产环境问题，例如 `hotfix/correct-payment-amount-error`。
*   **发布分支 (Release)**：`release/版本号`，用于准备新版本的发布，例如 `release/v1.2.0`。

## 提交信息规范

规范的提交信息不仅能让历史记录更易读，还能与语义化版本控制完美结合，推荐的格式如下：

```
<类型>[可选 范围]: <描述>

[可选 正文]

[可选 脚注]
```

其中，`<类型>` 是核心，必须是以下之一：
*   **`feat`**: 新增功能 (对应 **次版本号** 递增)
*   **`fix`**: 修复 Bug (对应 **修订号** 递增)
*   **`BREAKING CHANGE`** 或在类型后加 `!`: 引入了破坏性变更 (对应 **主版本号** 递增)
*   其他类型：`docs` (文档)、`style` (代码风格)、`refactor` (重构)、`test` (测试)、`chore` (构建过程或辅助工具变动) 等。

**示例**：
*   `feat: 添加用户注册功能`
*   `fix(api): 修复获取用户信息接口的空指针异常`
*   `feat!: 移除已废弃的v1接口，升级到v2`
