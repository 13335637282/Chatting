# 版本命名
## 版本号规范

`主版本号.次版本号.修订号`，其中 x 表示主版本号，y 表示次版本号，z 表示修订号。

### 版本号递增规则

| 版本部分               | 何时递增                     | 其他部分如何处理                | 示例                    |
|:-------------------|:-------------------------|:---------------------------|:----------------------|
| **主版本号 (MAJOR)**   | 进行了不兼容的 API 修改或重大变更时。    | 次版本号和修订号重置为 0          | 从 `1.2.3` 变为 `2.0.0`  |
| **次版本号 (MINOR)**   | 增加了新功能，但保持向后兼容时。         | 修订号重置为 0                 | 从 `1.2.3` 变为 `1.3.0`  |
| **修订号 (PATCH)**    | 进行了向后兼容的问题修正 (bug 修复) 时。 | 其他部分保持不变                 | 从 `1.2.3` 变为 `1.2.4`  |

通过添加后缀来表示预发布版本，例如 `1.0.0-alpha.1`（内部测试）、`1.0.0-beta.1`（公开测试）、`1.0.0-rc.1`（发布候选版本）。

## 分支命名规则

分支名应该清晰地表达其用途，通常采用 `前缀/描述` 的格式。

| 分支类型               | 前缀                | 命名格式                      | 示例                                      | 用途                                |
|:-------------------|:------------------|:---------------------------|:----------------------------------------|:-----------------------------------|
| **功能分支 (Feature)**   | `feature`         | `feature/简短的功能描述`         | `feature/user-login-api`、`feature/add-shopping-cart` | 开发新功能                              |
| **错误修复分支 (Bugfix)** | `bugfix`          | `bugfix/描述的问题`            | `bugfix/fix-navbar-overflow`             | 修复非紧急的 bug                         |
| **热修复分支 (Hotfix)**   | `hotfix`          | `hotfix/描述的问题`            | `hotfix/correct-payment-amount-error`    | 紧急修复生产环境问题                         |
| **发布分支 (Release)**    | `release`         | `release/版本号`              | `release/v1.2.0`                         | 准备新版本的发布                           |

## 开发步骤与版本管理流程

1.  **需求分析与规划**：确定新功能或修复的范围，规划版本号变更。
2.  **创建分支**：根据任务类型创建相应的分支（feature、bugfix、hotfix、release）。
3.  **开发与提交**：在分支上进行开发，遵循提交信息规范。
4.  **代码审查**：提交 PR 进行代码审查。
5.  **测试**：确保功能正常且无回归。
6.  **合并分支**：将开发分支合并到主分支。
7.  **版本发布**：
    *   更新版本号
    *   生成 Changelog.md
    *   打标签（tag）
    *   发布版本

## 提交需求详细说明

1.  **提交频率**：建议小步提交，每次提交解决一个具体问题。
2.  **提交信息要求**：
    *   第一行：简短描述（不超过 50 字符）
    *   空行
    *   详细描述（可选，解释为什么做这个变更，如何解决问题）
    *   空行
    *   脚注（可选，如 `BREAKING CHANGE: ...` 或 `Closes #123`）
3.  **提交类型使用指南**：

| 提交类型   | 描述                         | 对应版本号变更        | 示例                           |
|:-------|:---------------------------|:-----------------|:------------------------------|
| `feat`  | 新功能                        | 次版本号递增           | `feat: 添加用户注册功能`              |
| `fix`   | bug 修复                      | 修订号递增            | `fix(api): 修复获取用户信息接口的空指针异常` |
| `docs`  | 文档更新                       | 无                | `docs: 更新 API 文档`              |
| `style` | 代码风格调整（不影响功能）             | 无                | `style: 统一代码缩进`                |
| `refactor` | 代码重构（不添加功能或修复 bug）       | 无                | `refactor: 优化登录逻辑`             |
| `test`  | 测试相关                       | 无                | `test: 添加单元测试`                |
| `chore` | 构建过程或辅助工具变动                | 无                | `chore: 更新依赖版本`                |
| `BREAKING CHANGE` | 破坏性变更                     | 主版本号递增           | `feat!: 移除已废弃的 v1 接口，升级到 v2` |

## Changelog.md 生成规范

### 格式

```markdown
# Changelog

## [版本号] - YYYY-MM-DD

### Added
- 新增功能 1
- 新增功能 2

### Changed
- 变更功能 1
- 变更功能 2

### Fixed
- 修复问题 1
- 修复问题 2

### Removed
- 移除功能 1
- 移除功能 2

## [上一个版本号] - YYYY-MM-DD
...
```

### 生成方法

1.  **手动生成**：根据提交记录手动编写。
2.  **工具生成**：使用工具如 `standard-version`、`conventional-changelog` 等自动生成。
3.  **提交信息驱动**：基于符合规范的提交信息自动生成。

### 注意事项

| 注意事项 | 说明                         |
|:-----|:---------------------------|
| 排序方式 | 按时间倒序排列版本，最新版本在前           |
| 日期格式 | 每个版本包含明确的日期，格式为 YYYY-MM-DD   |
| 分类清晰 | 按照 Added、Changed、Fixed、Removed 分类 |
| 描述要求 | 描述简洁明了，避免冗长的说明           |
| 破坏性变更 | 包含破坏性变更的说明，确保用户了解影响       |

## 版本发布流程

1.  **准备发布**：
    *   检查所有功能是否完成
    *   运行测试确保无回归
    *   更新版本号
    *   生成 Changelog.md
2.  **发布**：
    *   打标签（tag）：`git tag -a v1.0.0 -m "Release v1.0.0"`
    *   推送标签：`git push origin v1.0.0`
    *   创建发布说明
3.  **后续操作**：
    *   通知相关人员
    *   更新文档
    *   准备下一版本的开发
